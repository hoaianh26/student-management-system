{% extends 'base.html.twig' %}

{% block title %}Add New Course{% endblock %}

{% block body %}
<div class="card shadow-sm border-0">
    <div class="card-header bg-warning py-3 text-dark">
        <h4 class="mb-0"><i class="bi bi-book"></i> Add New Course</h4>
    </div>
    <div class="card-body p-4">
        {{ include('course/_form.html.twig', {'button_label': 'Create Course'}) }}
    </div>
</div>
{% endblock %}
