{% extends 'base.html.twig' %}

{% block title %}Add New Department{% endblock %}

{% block body %}
<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="{{ path('department_index') }}">Departments</a></li>
    <li class="breadcrumb-item active" aria-current="page">Add New</li>
  </ol>
</nav>

<div class="card shadow-sm border-0">
    <div class="card-header bg-success text-white py-3">
        <h4 class="mb-0"><i class="bi bi-plus-circle"></i> Add New Department</h4>
    </div>
    <div class="card-body p-4">
        {{ include('department/_form.html.twig', {'button_label': 'Create Department'}) }}
    </div>
</div>
{% endblock %}
